services:
  - type: web
    name: resume-analyzer
    env: docker
    region: ohio
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: resume-analyzer-db
          property: connectionString

databases:
  - name: resume-analyzer-db
    region: ohio
    plan: free
